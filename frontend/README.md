# Intelligent Tourism Frontend

Minimal scaffold for the frontend using React + Vite + Tailwind.

Quick start:

```powershell
cd frontend
npm install
npm run dev
```

Build:

```powershell
npm run build
npm run preview
```
